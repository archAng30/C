#include <stdio.h>

int userInput(void);

int main(void) 
{
    printf("userInput value: %d", userInput());
    
    return 0;
}

int userInput(void)
{
    int i;
    
    printf("Insert integer: ");
    scanf("%d", &i);
    
    return (i);
}
